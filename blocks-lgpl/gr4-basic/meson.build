subdir('vector_source')
subdir('vector_sink')

#TODO: copy .hpp files into a common include directory with the appropriate structure

gr4_basic_h_dep = declare_dependency(
    include_directories: '.', dependencies : [fmt_dep, reflcpp_dep]
)